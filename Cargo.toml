[package]
name = "mutex-pool"
version = "0.1.0"
edition = "2024"

[dependencies]
crossbeam-utils = "0.8.21"
wyrand = "0.3.2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "count_pool"
harness = false
