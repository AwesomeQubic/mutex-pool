[package]
name = "mutex-pool"
version = "0.1.0"
edition = "2024"

[dependencies]
atomic-wait = "1.1.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "count_pool"
harness = false